import { SignedPayload } from "..";

describe('abc', () => {

    test('dfg', () => {
        let asd: SignedPayload = undefined;
        const x = 123;
        const y = 123;
        expect(x).toEqual(y);
    });
});
