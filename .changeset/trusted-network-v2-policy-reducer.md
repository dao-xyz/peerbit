---
"@peerbit/trusted-network": patch
---

Add an internal, non-activatable TrustedNetwork v2 authenticated policy-chain reducer with bounded pending state, fail-closed resolver unavailability, deterministic role projection, and canonical fork detection evidence.
